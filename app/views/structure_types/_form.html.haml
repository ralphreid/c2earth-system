= form_for @structure_type do |f|
  - if @structure_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@structure_type.errors.count, "error")} prohibited this structure_type from being saved:"
      %ul
        - @structure_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
