= form_for @stakeholder_type do |f|
  - if @stakeholder_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@stakeholder_type.errors.count, "error")} prohibited this stakeholder_type from being saved:"
      %ul
        - @stakeholder_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :name
    = f.text_field :name
  .actions
    = f.submit 'Save'
