= form_for @company_type do |f|
  - if @company_type.errors.any?
    #error_explanation
      %h2= "#{pluralize(@company_type.errors.count, "error")} prohibited this company_type from being saved:"
      %ul
        - @company_type.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :company_type
    = f.text_field :company_type
  .actions
    = f.submit 'Save'
